/* eslint-disable no-magic-numbers */
import DimensionHelper from 'Helpers/DimensionHelper'

export default {
  vSmall: DimensionHelper.normalize(12),
  small: DimensionHelper.normalize(14),
  normal: DimensionHelper.normalize(16),
  large: DimensionHelper.normalize(18),
  vLarge: DimensionHelper.normalize(24)
}
