export const navigationPropsMock = {
  navigate: jest.fn(),
  goBack: jest.fn(),
  getParam: jest.fn(),
  push: jest.fn(),
  dispatch: jest.fn(),
  setParams: jest.fn(),
  pop: jest.fn(),
  addListener: jest.fn(),
  isFocused: jest.fn(),
  toggleDrawer: jest.fn(),
  setOptions: jest.fn(),
  state: {
    params: {

    },
    routeName: 'Testing'
  }
}
